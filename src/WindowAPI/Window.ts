import * as PIXI from "pixi.js";

export class WindowOptions{
    public closeButton = true;
    public minimizeButton = true;
    public maximizeButton = true;
}
export class Window{
    public constructor(app:PIXI.Application, x:number, y:number, width:number, height:number, windowOptions:WindowOptions) {
        
    }
}